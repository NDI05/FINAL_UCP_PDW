-- Raja: additional schema for public article pages
-- Note: main schema is in ridha/database/schema.sql
-- This file contains Raja-specific extensions if needed

-- Contact form submissions are stored in contacts table (see ridha schema)
-- Public articles query from the articles table (see ridha schema)
-- No additional tables needed — Raja consumes the articles table
